<!--text-->
Len
===

```eppabasic
Function Len(text As String) As Integer
```

Returns the number of characters in the string `text`.

Example
---------
```eppabasic
Print Len("aybabtu")        ' 7
```
